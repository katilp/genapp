extends layout


block header 
  #header 
    h1= title
    p Welcome to #{title}

block content 
  #content 

    block row
      .row
        block column 
          .column  
            h3 Add a to-do item
            block myform 
              form(name="todo", method="post")
                div.input
                    span.label To-do item:
                    input(type="text", name="todoitem", cols="140")
                div.actions
                    input(type="submit", value="Add to-do") 
            p Added: #{todo}              
            //- ul
            //-   each val, index in [#{todo}, 'one', 'two']
            //-     li= index + ': ' + val     
          .column
            img(src="/images/shoe.jpg" width="460")

  